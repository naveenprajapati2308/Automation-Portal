# Testrix Shell (planned — Phase 3)

Master frontend of the platform, served at `/` behind the gateway.

Owns: login, global dashboard (aggregated product stats), product launcher
sidebar, central admin console, profile, notifications, AI chat panel.

Seeded from the Automation Portal frontend's auth/admin/layout components —
see docs/testrix-architecture-plan.md §4.

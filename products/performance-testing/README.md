# Performance Testing (future product)

Placeholder. Will follow the same shape as products/api-testing:
`backend/` (Spring Boot) + `frontend/` (React) + own schema + own compose file,
consuming platform auth and exposing `/api/v1/summary` to the global dashboard.

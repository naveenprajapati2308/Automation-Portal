import { test, expect } from '@playwright/test';

// Replace this with your real tests — this one only proves the Testrix connection works.
test('sample test connects to Testrix', async () => {
  expect(1 + 1).toBe(2);
});

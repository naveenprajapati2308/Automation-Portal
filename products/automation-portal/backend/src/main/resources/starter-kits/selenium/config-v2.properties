# Filled in for you by Testrix's "Download Starter Kit" — these are your workspace's own values.
# ConfigUtils.java reads these, but a -D system property of the same name always wins (that's how
# Testrix's own Execution Center overrides them for a real run).
portal.url={{PORTAL_URL}}
portal.api.key={{API_KEY}}
framework.path={{FRAMEWORK_PATH}}
report.path={{REPORT_PATH}}
